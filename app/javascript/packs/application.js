import "bootstrap";

const url = window.location;

// $(document).ready(() => {
//   if (url.pathname === '/') {
//     const text = ['H', 'A', 'Z', 'E'];
//     let counter = 0;
//     const interval = setInterval(() => {
//       $('#landing__title').append(`<span class="landing__letter">${text[counter]}</span>`);
//       if (counter === 3) {
//         clearInterval(interval);
//       }
//       counter++
//     }, 1000)
//   }
// })
