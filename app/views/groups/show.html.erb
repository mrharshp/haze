<div class="header3">
  <h3><%= link_to "<", groups_path(@group) %></h3>
  <h3 class="uppercase"><%= link_to @group.name, edit_group_path(@group) %></h3>
</div>
<div class="d-flex m-2">
  <button class="btn btn-primary w-50">Expenses</button>
  <%= link_to "Lists", group_lists_path(@group), class: "btn btn-primary w-50" %>
</div>
