<% @list_items.each do |item| %>
<div class="card-product d-flex align-items-center">
    <% if item.photo.url.nil? %>
      <div style="display: none;"></div>
      <a href="http://<%= item.url %>">Check this out!</a>
    <% else %>
      <div>
        <a href="http://<%= item.url %>"><%= cl_image_tag item.photo, crop: :fill %></a>
      </div>
    <% end %>
    <div class="card-product-infos flex-grow-1">
        <% if item.user == current_user %>
          <div>
            <h2><%= link_to item.title, edit_group_list_list_item_path(@group, @list, item) %></h2>
            <p><%= item.description %></p>
            <p>Added by <%= item.user.name %></p>
          </div>
        <% else %>
          <div>
            <h2><%= item.title %></h2>
            <p><%= item.description %></p>
            <p>Added by <%= item.user.name %></p>
          </div>
        <% end %>
    </div>
    <div>
      <div class="d-flex mr-2">
        <span class="mr-1" id="upvote-<%= item.id %>"><%= item.upvote %></span>
        <%= link_to  upvote_list_item_path(item), remote: true, method: :post do %>
          <i class="far fa-thumbs-up"></i>
        <% end %>
      </div>
      <div class="d-flex mr-2">
        <span class="mr-1" id="downvote-<%= item.id %>"><%= item.downvote %></span>
        <%= link_to  downvote_list_item_path(item), remote: true, method: :post do %>
          <i class="far fa-thumbs-down"></i>
        <% end %>
      </div>
    </div>
</div>
<% end %>
